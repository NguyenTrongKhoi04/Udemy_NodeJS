1. node 
    - tab 
    - (Ctrl + D) || (Ctrl + C) = .exit
    - Ctrl + Shift + Enter = .execute
    - 4+5 > _+1 ("_" lấy kết quả trước đó)
    - String. + (tab x 2)

2 npm
    - npm init 
    - npm run start (config "start" trong "scripts")
    - npm install nodemon --save-dev
    - npm i nodemon --global
    - npm install slugify
    - npm outdated
    - npm install slugify@1.0.0 (update lại chỉ 1 package với ver cụ thể)